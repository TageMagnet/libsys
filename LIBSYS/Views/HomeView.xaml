<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:LIBSYS.ViewModels;assembly=LIBSYS"
             mc:Ignorable="d" d:DesignWidth="960" d:DesignHeight="450"
             x:Class="LIBSYS.Views.HomeView" FontWeight="DemiBold" Foreground="#344955">
  
  
  <Grid ColumnDefinitions="1*, 1*, 1*" RowDefinitions="1* 1* 1* ">

    <TextBox Watermark="Sök Bok, E-Bok eller Seminarie" Grid.Column="1" Grid.Row="0" BorderBrush="#F9AA33" Height="30" VerticalAlignment="Top" Margin="0 30 0 0" MinWidth="200" MaxWidth="500" />
    
    
    
    <StackPanel Grid.Column="2" Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="20">
      <Button Command="{Binding LoginCommand}" Content="Login" Width="100" Height="50" Margin="5" Background="#F9AA33" Foreground="#344955"/>
      <Button Command="{Binding RegisterCommand}" Content="Register" Width="100" Height="50" Margin="5" Background="#F9AA33" Foreground="#344955"/>
    </StackPanel>
    
  </Grid>
</UserControl>
