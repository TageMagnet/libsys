<UserControl x:Class="LibrarySystem.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibrarySystem.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignWidth="800">
    <Border Background="Gray">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="6"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="6"/>
            </Grid.RowDefinitions>

            <!--Go Home Button-->
            <Button Grid.Column="0" Grid.Row="1" Name="BtnIcon" Width="36" Padding="6,6" Content="{materialDesign:PackIcon Home}" ToolTip="Icon"/>

            <!--Image-->

            <Image Source="/Assets/LibSysWhite.png" Stretch="Fill" Grid.Column="1" Grid.Row="1" MaxWidth="64"/>

            <StackPanel Grid.Column="2" Grid.Row="1" Orientation="Horizontal">
                <Button Command="{Binding LoginCommand}" 
                        Content="{materialDesign:PackIcon Login}" 
                        Width="36"
                        Margin="12,0,0,0"
                        Padding="6,6"
                        />
                <Button Command="{Binding RegisterCommand}" 
                        Content="{materialDesign:PackIcon Register}" 
                        Width="36"
                        Margin="12,0,0,0"
                        Padding="6,6"
                        />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
