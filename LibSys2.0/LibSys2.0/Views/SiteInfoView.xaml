<Window x:Class="LibrarySystem.Views.SiteInfoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibrarySystem.Views"
        mc:Ignorable="d"
        Title="SiteInfoView" Height="450" Width="800">
    <Grid>
        <Grid>
            <StackPanel>
                <Label Content="Info"/>
                <!--#region Book Info -->
                <TextBlock Text="{Binding BookInfo}"/>

                <!--#region  Register Info -->
                <TextBlock Text="{Binding RegisterInfo}"/>

                <!--#region  General Info -->
                <TextBlock Text="{Binding GeneralInfo}"/>
            </StackPanel>

        </Grid>
    </Grid>
</Window>
